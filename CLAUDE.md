# CLAUDE.md — SweatDash (50-SaaS #55)

This repo is product #55 of Febin's 50-SaaS challenge. Nothing is built yet — `docs/` is the source of truth.

Read `docs/LLD.md` then `docs/PLAN.md` and execute tasks in order with TDD.

Stack conventions and the shipped reference implementation live in the private repo `febufenn-cyber/50-saas` (`contract-reviewer/`) — same Worker + Supabase + credits patterns, adapted here to WhatsApp inbound logging and a subscription gate.

The Workers AI Whisper transcription choice, the no-diet/calorie wellbeing guardrail on every LLM call, the lighter WhatsApp gate (inbound replies need no template; only the weekly push does), and the subscription-over-credits monetization deviation in the LLD are verified decisions — do not re-litigate without evidence.
